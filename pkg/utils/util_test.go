package utils

import (
	"testing"
)

func TestAAA(t *testing.T) {

	//var a = "todoList"
	//assert.Equal(t, inflect.Camelize(a),"todolist","camelize")
	//assert.Equal(t, inflect.CamelizeDownFirst(a),"todolist","camelize d")

	AAA()
}
